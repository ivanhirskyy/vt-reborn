{
  "name": "vt",
  "type": "module",
  "private": true,
  "scripts": {
    "in": "bun run src/punch.ts in",
    "out": "bun run src/punch.ts out",
    "start": "bun run src/scheduler.ts",
    "backfill": "bun run src/backfill.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node-cron": "^3.0.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "node-cron": "^4.2.1"
  }
}
